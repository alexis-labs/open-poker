# Blind badges

Drop badge art here:

- `small.png`
- `big.png`
- `boss.png`

Recommended size: 128 × 128 (round-cropped). Wire-up: see the `.blind-badge`
element in `index.html` — point `background-image` at `/art/blinds/{kind}.png`.
